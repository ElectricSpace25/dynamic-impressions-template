export const config = {
    // Total number of videos the participant will see (will be randomly split between conditions) 
    num_of_videos: 10,

    // When enabled, logs debug messages to console
    DEBUG_LOGS: false,

    // When enabled, skips instructions and demographics
    DEBUG_QUICK: false,

    // When enabled, saves data locally
    DEBUG_SAVE: false
};